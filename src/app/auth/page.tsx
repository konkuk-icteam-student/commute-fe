import { AuthPanel } from "@/features/auth";

export default function LoginPage() {
  return (
    <main className="min-h-screen w-full max-w-150 shadow-[0_0_10px_4px_rgba(0,0,0,0.04)]">
      <AuthPanel />
    </main>
  );
}
